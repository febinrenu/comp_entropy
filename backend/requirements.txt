# Core Backend
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6
websockets==12.0

# Database
sqlalchemy==2.0.25
aiosqlite==0.19.0
asyncpg==0.29.0  # async Postgres driver, used indirectly via DATABASE_URL when pointed at Postgres

# Data Science & Analysis
numpy==1.26.3
scipy==1.12.0

# Energy Monitoring
# codecarbon imports pkg_resources internally without declaring
# setuptools as its own dependency; setuptools>=81 removed pkg_resources
# entirely, which breaks codecarbon's import at runtime (falls back to
# TDP-only estimates, silently degrading a "real" energy source to a
# proxy). Pin setuptools<81 until codecarbon fixes this upstream.
setuptools<81
codecarbon==2.3.4
psutil==5.9.8
GPUtil==1.4.0
pynvml==11.5.0

# LLM Integration
# Ollama (default provider) is called over HTTP via httpx below -- no
# separate client package needed. openai/anthropic remain as alternate
# providers.
openai==1.3.9
anthropic==0.39.0

# NLP & Text Processing
textstat==0.7.3

# Validation & Serialization
pydantic==2.5.3
pydantic-settings==2.1.0
python-dotenv==1.0.0  # used by pydantic-settings to load .env (Settings.Config.env_file)

# Testing
pytest==7.4.4
pytest-asyncio==0.23.3
pytest-cov==4.1.0
httpx==0.26.0

# Utilities
loguru==0.7.2
